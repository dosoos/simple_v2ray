# 由 ./scripts/init 生成 .env；以下为字段说明。

# 站点域名（可多域名，逗号分隔）；面板分享链接取第一个域名
HOST_DOMAIN=localhost

# Caddy Basic Auth 用户名（须与面板右上角一致）
CADDY_ADMIN_USER=admin

# 管理密码写入 caddy/Caddyfile 的 basicauth 块（bcrypt），不放在本文件。
# 修改密码: ./scripts/passwd
