# Caddy 站点名与 TLS；可多域名，逗号分隔即可（如 a.com,b.com）；compose 会转成 Caddy 要求的「逗号后空格」。面板分享链接取第一个域名。
HOST_DOMAIN=localhost

# Caddy Basic Auth（保护 /panel 含 /panel/api 与 /assets）
# 用户名（须与面板右上角一致）
CADDY_ADMIN_USER=admin
# 密码不再通过本文件传递（bcrypt 里的 $ 会被 Compose 破坏）。
# 请使用仓库内 caddy/admin.hash（单行哈希）或运行：
#   ./scripts/gen-caddy-hash.sh '你的强密码'
# 默认 admin.hash 对应密码为 changeme（仅试跑）。

# （可选）Stats API / 按月流量 / 重启容器目标：应用内有默认值；覆盖方式示例——本地 export V2RAY_API_HOST=…，或在 docker-compose.yml 的 panel.environment 追加一行
