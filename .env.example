# Caddy 站点名与 TLS；可多域名，逗号分隔即可（如 a.com,b.com）；compose 会转成 Caddy 要求的「逗号后空格」。面板分享链接取第一个域名。
HOST_DOMAIN=localhost

# Caddy Basic Auth（保护 /panel 含 /panel/api 与 /assets）
# 用户名
CADDY_ADMIN_USER=admin
# 密码的 bcrypt 哈希（不要用明文）。生成方式：
#   ./scripts/gen-caddy-hash.sh '你的强密码'
# 或：
#   docker run --rm caddy:2-alpine caddy hash-password --plaintext '你的强密码'
# 将输出的哈希粘贴在下一行。留空时 compose 内建默认（密码为 changeme，仅用于试跑）
# 示例（密码为 admin）：整段建议单引号包裹 —— CADDY_ADMIN_HASH='$2a$14$......'
CADDY_ADMIN_HASH=

# （可选）Stats API / 按月流量 / 重启容器目标：应用内有默认值；覆盖方式示例——本地 export V2RAY_API_HOST=…，或在 compose 的 panel.environment 追加一行
