# 面板对外域名（用于订阅链接与 Caddy TLS）
HOST_DOMAIN=example.com

# Django
DJANGO_SECRET_KEY=请替换为随机字符串
DEBUG=False

# 多节点：留空表示本机为主节点；从节点填写主节点完整 URL（https://主域名）
MASTER_URL=

# 可选：本机节点显示名称（默认 node-1）；多从节点时每台建议不同，且须与同步 API 中的名称一致
# NODE_NAME=HK-1
